import { Section } from "./Section"

export const Footer =()=>{
    return <footer className="bg-card">
        <Section className="py-8">
            <p className="text-muted-foreground text-sm  flex justify-center">@Copywrite 2024 Mathilde Besse</p>
        </Section>
    </footer>
}